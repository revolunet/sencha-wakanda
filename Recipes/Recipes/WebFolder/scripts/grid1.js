WAF.onAfterInit = function onAfterInit() {// @lock// @region namespaceDeclaration// @startlock	var extGridPanel0 = WAF.namespace("Recipes.grid1.extGridPanel0.events");	// @extGridPanel// @endregion// @endlock// eventHandlers// @startlock	extGridPanel0.itemmouseenter = function grid0_itemmouseenter (event)	{// @endlock		var record= arguments[1];		if (!record) return; 		$$('textField1').setValue(record.get('ID'));	};// @lock	extGridPanel0.select = function grid0_select (event)// @startlock	{// @endlock		console.log('SELECT', arguments);		var record= arguments[1];		if (!record) return;		//alert('SELECT ITEM  '+record.get('ID') );		 	};// @lock	extGridPanel0.itemdblclick = function grid0_itemdblclick (event)// @startlock	{// @endlock		console.log('DBL', arguments);		var record= arguments[1];		if (!record) return;		alert('dbl click on '+record.get('ID') );			};// @lock	extGridPanel0.selectionchange = function grid0_selectionchange (event)// @startlock	{// @endlock		var records= arguments[1];		if (!records) return;		//console.log('CLICKED INDEX ', record, record.index)		$$('textField0').setValue(records.length);	};// @lock// @region eventManager// @startlock	WAF.addListener("extGridPanel0", "itemmouseenter", extGridPanel0.itemmouseenter, "WAF");	WAF.addListener("extGridPanel0", "select", extGridPanel0.select, "WAF");	WAF.addListener("extGridPanel0", "itemdblclick", extGridPanel0.itemdblclick, "WAF");	WAF.addListener("extGridPanel0", "selectionchange", extGridPanel0.selectionchange, "WAF");// @endregion};// @endlock